# Core scientific stack (pinned to a local Python 3.10.20 + conda-forge env
# used to regenerate splits). GPU wheels are platform-specific: prefer
# scripts/setup_env.sh on Linux CUDA hosts (torch==2.4.1, torch-geometric==2.6.1,
# scikit-learn==1.5.2, xgboost==2.1.3).
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.6.1
scipy==1.15.3
rdkit==2024.09.2
tqdm==4.67.1
pyyaml==6.0.2

# Deep learning — install the CUDA wheel that matches your driver first:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# then:
torch>=2.4.1
torch-geometric>=2.6.1
xgboost==3.0.2

# Analysis
matplotlib==3.10.8
seaborn==0.13.2
shap==0.48.0
requests==2.32.3
